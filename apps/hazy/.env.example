NEXT_PUBLIC_APP_URL=http://localhost:3100
# hazy-note app (separate deploy, same DB) — "Open in Note" deep link on the item page
NEXT_PUBLIC_HAZY_NOTE_URL=http://localhost:3000

# The hazy API service (apps/api). Local dev: http://localhost:8787.
# Production: https://api.hz.nknighta.me
NEXT_PUBLIC_API_URL=http://localhost:8787

# Clerk — https://clerk.com (free Hobby tier). Create an app, copy the keys
# from the API Keys page. The webhook + database + OpenRouter now live in
# apps/api, not here.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
CLERK_SECRET_KEY=
